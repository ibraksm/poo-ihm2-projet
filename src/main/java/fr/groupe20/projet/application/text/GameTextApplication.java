package fr.groupe20.projet.application.text;

import java.util.Scanner;

public class GameTextApplication extends TextApplication {
    @Override
    public void run(String[] args) throws Exception {
        Scanner input = new Scanner(System.in);

        //Load levels
        //Montrer les levels (num - Nom)
        //Commande pour select un level

        //Quand dans un level commandes pour bouger le personnage

        input.close();
    }
}
